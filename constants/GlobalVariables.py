from enum import Enum

class MainButton(Enum):
    ADDITION = '+'
    SUBTRACTION = '-'
    MULTIPLICATION = '*'
    DIVISION = '/'

class AdditionalButton(Enum):
    SQRT = 'sqrt'
    REMAINDER = '%'
    POWER = '^'

extended = 1
